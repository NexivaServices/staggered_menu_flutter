name: staggered_menu_flutter_example
description: Example app for the staggered_menu_flutter package.
publish_to: none
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  staggered_menu_flutter:
    path: ../

flutter:
  uses-material-design: true
